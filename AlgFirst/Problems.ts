const myArr = (arr: number[], value: number) => {
	let res: number[][] = [];
	let hashtable: number[] = [];

	for (let i = 0; i < arr.length; i++) {
		let counterNum = arr[i];
	}
};

let data1 = [3, 6, 8];

console.log(myArr(data1, 7));
